# Быстрый старт - Ollama Demo

## Что было создано

Создан полный пример работы с библиотекой `llm_tags` в виде Jupyter notebook с тремя этапами обработки данных.

### Файлы:
- **`ollama_demo.ipynb`** - основной notebook с примером (32 ячейки)
- **`OLLAMA_DEMO_README.md`** - подробная документация на английском
- **`demo_sample.csv.gz`** - демо данные для обработки

## Три этапа обработки

### 📊 Этап 1: Первичная обработка всех данных
- Обрабатываются ВСЕ обращения из файла
- Создается базовый словарь тегов
- Результат: `results_stage1.csv`, `tags_dict_stage1.json`

### 🔍 Этап 2: Дополнительный анализ не тегнутых обращений
- Анализируются только обращения БЕЗ тегов
- Используется более детальный промпт
- Результат: `results_stage2.csv`, `tags_dict_stage2.json`

### ✅ Этап 3: Финальная переразметка всех обращений
- ПОЛНАЯ переразметка всех обращений с нуля
- Используется накопленный словарь тегов
- Результат: `results_final.csv`, `tags_dict_final.json`

## Как запустить

### Шаг 1: Запустите Ollama
```bash
# В отдельном терминале
ollama serve
```

### Шаг 2: Установите модель (если еще не установлена)
```bash
ollama pull qwen2.5:32b
```

### Шаг 3: Запустите notebook
```bash
cd /home/alex/tradeML/llm_tags
source venv/bin/activate
jupyter notebook ollama_demo.ipynb
```

### Шаг 4: Выполните ячейки последовательно
Просто запускайте ячейки одну за другой (Shift+Enter).

## Что происходит в notebook

### Подготовка (ячейки 1-6)
1. Импорт библиотек
2. Загрузка данных из `demo_sample.csv.gz`
3. Статистика по данным
4. Инициализация LLM и Pipeline
5. Подготовка промпта

### Этап 1 (ячейки 7-12)
1. Запуск первичного тегирования
2. Анализ результатов
3. Примеры размеченных обращений
4. Словарь тегов
5. Сохранение результатов

### Этап 2 (ячейки 13-18)
1. Поиск не тегнутых обращений
2. Повторная обработка с детальным промптом
3. Анализ улучшений
4. Сравнение с этапом 1
5. Сохранение результатов

### Этап 3 (ячейки 19-27)
1. Финальная переразметка всех обращений
2. Подробная статистика
3. Топ-20 тегов
4. Примеры разметки
5. Словарь тегов с частотой использования
6. Сравнение всех трех этапов
7. Сохранение результатов

### Анализ качества (ячейки 28-31)
1. Статистика по типу speaker (agent/client)
2. Анализ обращений без тегов
3. Итоги

## Настройки производительности

### Быстрая система (GPU, много RAM)
```python
pipeline = TaggingPipeline(
    llm=llm,
    batch_size=100,
    num_workers=10
)
```

### Средняя система (по умолчанию)
```python
pipeline = TaggingPipeline(
    llm=llm,
    batch_size=50,
    num_workers=5
)
```

### Медленная система
```python
pipeline = TaggingPipeline(
    llm=llm,
    batch_size=20,
    num_workers=2
)
```

## Ожидаемое время выполнения

Для 1000 обращений на средней системе:
- **Этап 1**: ~15 минут
- **Этап 2**: ~3 минуты (только не тегнутые)
- **Этап 3**: ~15 минут
- **Итого**: ~33 минуты

## Результаты

После выполнения всех ячеек у вас будет:

### CSV файлы с результатами:
- `results_stage1.csv` - после этапа 1
- `results_stage2.csv` - после этапа 2
- `results_final.csv` - финальные результаты ✅

### JSON файлы со словарями тегов:
- `tags_dict_stage1.json` - словарь после этапа 1
- `tags_dict_stage2.json` - словарь после этапа 2
- `tags_dict_final.json` - финальный словарь ✅

### Сравнительная таблица:
- `comparison.csv` - сравнение всех этапов

## Структура результатов

### Входные данные:
```csv
conversation_id,speaker,date_time,text,request_id
c32e822d...,agent,2023-09-07T08:12:21...,"текст обращения",demo-0000
```

### Выходные данные:
```csv
conversation_id,speaker,date_time,text,request_id,tags
c32e822d...,agent,2023-09-07T08:12:21...,"текст обращения",demo-0000,"обслуживание, консультация"
```

## Анализ в notebook

Notebook автоматически создает:
- 📊 Статистику по покрытию (сколько обращений получили теги)
- 🏆 Топ-20 самых популярных тегов
- 📈 Распределение по количеству тегов
- 👥 Анализ по типу speaker (agent vs client)
- 🔄 Сравнение результатов всех этапов
- 📝 Примеры размеченных обращений

## Частые вопросы

### Q: Нужно ли запускать все три этапа?
**A:** Да, этапы последовательные и каждый улучшает результаты предыдущего.

### Q: Можно ли использовать другую модель?
**A:** Да, измените параметр `model` в ячейке инициализации LLM:
```python
llm = OllamaLLM(model="qwen2.5:14b")  # или другая модель
```

### Q: Можно ли использовать свои данные?
**A:** Да, измените путь к файлу в ячейке загрузки данных:
```python
data_path = "/путь/к/вашим/данным.csv"
```

### Q: Что делать если Ollama не запускается?
**A:** Проверьте:
```bash
# Проверка статуса
ps aux | grep ollama

# Перезапуск
pkill ollama
ollama serve
```

### Q: Как ускорить обработку?
**A:** 
1. Используйте более легкую модель (qwen2.5:7b)
2. Увеличьте `batch_size` и `num_workers`
3. Используйте GPU если доступен

## Следующие шаги

После успешного выполнения notebook:

1. **Изучите результаты** в файлах `results_final.csv` и `tags_dict_final.json`
2. **Проанализируйте качество** разметки в последних ячейках notebook
3. **Настройте промпты** под ваши задачи
4. **Используйте свои данные** для реальной разметки

## Поддержка

Если что-то не работает:
1. Проверьте что Ollama запущена: `ps aux | grep ollama`
2. Проверьте что модель установлена: `ollama list`
3. Посмотрите логи в ячейках notebook
4. Обратитесь к `OLLAMA_DEMO_README.md` для подробной документации

---

**Успехов в работе с llm_tags! 🚀**

